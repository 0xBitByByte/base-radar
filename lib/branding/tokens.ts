/** Shared copy for `TokenLogo`'s reserved, no-fabrication empty state — see the doc comment on that component for why this is the common case today. */
export const TOKEN_LOGO_UNAVAILABLE_TITLE = "Token logo not yet available";
